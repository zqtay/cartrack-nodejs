const dotenv = require('dotenv');

var reqUserList = {};

function run(req){
	var ip = req.headers['x-forwarded-for'] ||
     req.socket.remoteAddress ||
     null;
	
}

var m = {
	
	
}

module.exports = m;